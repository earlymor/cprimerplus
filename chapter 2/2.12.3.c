//target:conver your age to days
#include<stdio.h>
int main(int argc,char **argv ){
    int age;
    printf("Input your age:");
    scanf("%d",&age);
    printf("The number of days is:%d ",age*365);
    return 0;
}